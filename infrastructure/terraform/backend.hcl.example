# Copy to backend.hcl and provide environment-specific values.
#
# terraform init -reconfigure -backend-config=backend.hcl

bucket         = "voxguard-terraform-state-prod"
key            = "multi-region/terraform.tfstate"
region         = "af-south-1"
encrypt        = true

dynamodb_table = "voxguard-terraform-locks"

# Optional hardening
# kms_key_id     = "arn:aws:kms:af-south-1:123456789012:key/00000000-0000-0000-0000-000000000000"
# profile        = "voxguard-prod"
