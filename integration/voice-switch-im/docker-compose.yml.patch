diff --git a/docker-compose.yml b/docker-compose.yml
index 9697ead..e1a20c2 100644
--- a/docker-compose.yml
+++ b/docker-compose.yml
@@ -239,6 +239,9 @@ services:
       # YugabyteDB specific optimizations
       YSQL_PREFER_FOLLOWER_READS: "true"
       YSQL_REQUEST_LIMIT: "1024"
+      # Fraud detection integration
+      FRAUD_DETECTION_URL: "http://fraud-detection:5000"
+      KAMAILIO_MI_URL: "http://kamailio-sbc:5060"
     ports:
       - "8080:8080"
     depends_on:
@@ -250,6 +253,8 @@ services:
         condition: service_started
       db-init:
         condition: service_completed_successfully
+      fraud-detection:
+        condition: service_healthy
     networks:
       private:
         ipv4_address: 172.16.238.11
@@ -264,6 +269,54 @@ services:
           memory: 512M
           cpus: '2'
 
+  # kdb+ Fraud Detection Service - Anti-Call Masking System
+  fraud-detection:
+    build:
+      context: ../Anti_Call-Masking/anti-call-masking
+      dockerfile: Dockerfile
+    environment:
+      # Detection parameters
+      DETECTION_WINDOW_SEC: "5"
+      DETECTION_THRESHOLD: "5"
+      # Switch integration
+      SWITCH_PROTOCOL: "voiceswitch"
+      SWITCH_HOST: "carrier-api"
+      SWITCH_PORT: "8080"
+      VOICE_SWITCH_API_URL: "http://carrier-api:8080/api/v1"
+      # Cache for state persistence
+      REDIS_URL: "redis://dragonfly:6379/1"
+      # Logging
+      LOG_LEVEL: "info"
+      LOG_FORMAT: "json"
+      # Metrics
+      METRICS_ENABLED: "true"
+      PROMETHEUS_PORT: "9090"
+      # Webhook for alerts
+      WEBHOOK_URL: "http://carrier-api:8080/api/v1/fraud/alerts/webhook"
+    ports:
+      - "5000:5000"   # HTTP API
+      - "9090:9090"   # Prometheus metrics
+    depends_on:
+      dragonfly:
+        condition: service_healthy
+    networks:
+      private:
+        ipv4_address: 172.16.238.20
+    healthcheck:
+      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
+      interval: 10s
+      timeout: 5s
+      retries: 5
+      start_period: 30s
+    deploy:
+      resources:
+        limits:
+          memory: 512M
+          cpus: '2'
+    volumes:
+      - fraud-detection-data:/app/data
+      - fraud-detection-logs:/app/logs
+
   # Frontend with nginx optimizations
   frontend:
     build:
@@ -310,3 +363,5 @@ volumes:
   yb-master-data:
   yb-tserver-data:
   dragonfly-data:
+  fraud-detection-data:
+  fraud-detection-logs:
