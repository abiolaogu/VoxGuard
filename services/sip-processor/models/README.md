# Placeholder XGBoost model
# In production, replace with your trained masking detection model
#
# To create a dummy model for testing:
# python -c "from app.inference.model import ModelManager; ModelManager.create_dummy_model()"
