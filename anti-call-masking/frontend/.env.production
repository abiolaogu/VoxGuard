# Production Environment Variables
# These should be set via CI/CD or container environment

# Hasura GraphQL Engine
VITE_HASURA_ENDPOINT=https://hasura.acm.example.com/v1/graphql
VITE_HASURA_WS_ENDPOINT=wss://hasura.acm.example.com/v1/graphql
# Note: In production, use JWT authentication instead of admin secret
# VITE_HASURA_ADMIN_SECRET should NOT be set in production frontend

# API Endpoints
VITE_API_BASE_URL=https://api.acm.example.com
VITE_ACM_ENGINE_URL=https://engine.acm.example.com

# Application Settings
VITE_APP_NAME=ACM Monitor
VITE_APP_VERSION=2.0.0
